import 'package:flutter/material.dart';

class AddMemoryViewBody extends StatelessWidget {
  const AddMemoryViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}